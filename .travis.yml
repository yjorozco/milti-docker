sudo: required
services:
  - docker

before_install:
  - docker build -t  yjorozco/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run yjorozco/react-test npm test -- --coverage

after_success: 
  - docker build -f multi-client ./client
  - docker build -f multi-nginx ./nginx
  - docker build -f multi-server ./server
  - docker build -f multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID"  --password-stdin
  - docker push multi-client
  - docker push multi-nginx
  - docker push multi-server
  - docker push multi-worker
